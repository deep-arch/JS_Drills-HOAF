import { items } from "../../Inventory/inventory.js";
import { reduce } from "../reduce.js";
const cb = (startingValue, currentValue) => 
{
  // startingValue + currentValue; *to test*
  //if(enter your condition)
  //{
      //Add your code
  //}
};
console.log(reduce(items,cb));